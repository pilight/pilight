include(../../../../CMakeConfig.txt)

if(${PROTOCOL_FS20} MATCHES "OFF")
	list(REMOVE_ITEM ${PROJECT_NAME}_sources "${PROJECT_SOURCE_DIR}/fs20.c")
	list(REMOVE_ITEM ${PROJECT_NAME}_headers "${PROJECT_SOURCE_DIR}/fs20.h")
endif()
